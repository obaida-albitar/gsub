# Test, lint, and build tooling for developing gsub.
# Runtime dependencies come from the package itself: `pip install -e .`
# System build tools (not pip-installable): blueprint-compiler and
# glib-compile-resources (from the gtk4 / libadwaita dev packages).
# Runtime system libraries (not pip-installable): libadwaita-1, gtk4, libmpv.

pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0
# Pinned exactly: newer ruff releases can enable additional default rules,
# which would make lint results differ between machines.
ruff==0.15.16

# Compiles data/blueprints/*.blp at pip-install time (setup.py). Also
# available from distributions as `blueprint-compiler`; either works.
blueprint-compiler

# Canonical system build (alternative to pip)
meson>=1.0.0
